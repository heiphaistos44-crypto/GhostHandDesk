{
  "name": "ghosthanddesk-client-wrapper",
  "version": "0.5.1",
  "private": true,
  "description": "Wrapper package for Tauri",
  "scripts": {
    "dev": "cd ui && npm run dev",
    "build": "cd ui && npm run build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0"
  }
}
